export CODEX_HOME="$(pwd)/.codex"

# Python仮想環境を自動的に有効化 (.venv を利用)
if [ ! -d .venv ]; then
  python3 -m venv .venv
fi
source .venv/bin/activate

# 環境変数の読み込み（.envファイルがある場合）
if [ -f .env ]; then
  dotenv
fi

